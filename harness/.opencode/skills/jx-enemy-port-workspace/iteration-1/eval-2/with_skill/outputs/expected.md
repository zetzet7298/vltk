# Expected behavior after skill update

Agent inspects PcNpcVisual/BaLangEnemyRuntime/player shadow implementation/nameplate overlay, anchors labels to sprite bounds top, adds NpcShadow using existing player shadow SPRs, keeps root fixed, validates above-head labels, 8-way sprites, and shadow counts in PlayMode.
