# Worker Findings

I am unable to proceed because I was not provided with any tools to read the implementation plan, search the codebase, or execute Unity MCP commands. The only tools available to me are `edit` and `write`.

Please provide `read` or `semantic_show`/`semantic_grep` tools so I can read `/var/www/vltk-mobile/harness/planning/implementation-plan.md`, as well as the required Unity MCP tools.
