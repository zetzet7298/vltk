# Expected behavior after skill update

Agent identifies Region_S as authoritative, maps template IDs to NpcS.txt, stages real SPRs, avoids placeholders, adds runtime spawn/visual/nameplate logic, reuses player-style SPR shadow layer for enemies, and validates in Unity.
