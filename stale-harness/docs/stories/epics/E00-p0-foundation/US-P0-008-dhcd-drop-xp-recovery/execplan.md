# Exec Plan

## Goal

Recover exact active drop/XP data and formulas or maintain a reproducible blocked boundary.

## Scope

In scope: hash-locked candidate catalogs, VFS/update selection, valid decryption/parser acceptance, native reward traces, evidence and verifier.

Out of scope: balance tuning, reward implementation, and JX source.

## Risk Classification

Risk flags: economy contract, external encrypted assets, cross-platform runtime behavior.

Hard gates: index inputs must be hash-locked; active bytes and arithmetic require independent corroboration; candidate rows are not constants.

## Work Phases

1. Add exact index-3/5/7 provenance and normalize candidate rows.
2. Recover selected VFS/update path and build key.
3. Parse selected tables and map native reward methods.
4. Corroborate with authorized runtime evidence.
5. Run verifier and Harness gates.

## Stop Conditions

Stop when candidate membership, key, active winner, or parser acceptance is missing; do not publish reward numbers.
