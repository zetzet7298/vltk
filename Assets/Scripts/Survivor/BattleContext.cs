namespace VLTK.Survivor
{
    /// <summary>
    /// Context shared across survivor systems. parity dhcd BattleContext
    /// (player entity mgr / config mgr / global attr). MVP: empty holder,
    /// will grow as systems port in (P1+).
    /// </summary>
    public sealed class BattleContext
    {
    }
}
