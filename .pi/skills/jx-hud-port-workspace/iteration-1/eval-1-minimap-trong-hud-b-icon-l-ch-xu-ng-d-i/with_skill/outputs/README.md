# Eval 1

Prompt:
Minimap trong HUD bị icon lệch xuống dưới khung, với nhóm nút dưới đè joystick. Fix để giống PC và joystick vẫn kéo được.

Expected:
Agent should move minimap buttons inside the minimap frame, avoid UI Toolkit pointer capture over joystick, and verify no overlap.

Not executed in this packaging pass; eval prompts are staged for future skill regression testing.
